<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flytoyou.srmanager">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_FORMAT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".Util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Index">

        </activity>

        <activity android:name=".Login">

        </activity>

        <activity android:name=".Sign">

        </activity>

        <activity android:name=".Talk_All">

        </activity>

        <activity android:name=".ResPost">

        </activity>

        <activity android:name=".SelectLogin">

        </activity>

        <activity android:name=".MyMessage">

        </activity>

        <activity android:name=".LoginPage">

        </activity>

        <activity android:name=".SexSign">

        </activity>

        <activity android:name=".JobSign">

        </activity>

    </application>

</manifest>